# scripts

| Script | Purpose |
|---|---|
| `run_monitor.py` | Local dev entry-point that runs the live-monitor loop. Equivalent to the `afp-monitor` console script. |

Run with:

```bash
python scripts/run_monitor.py
```
