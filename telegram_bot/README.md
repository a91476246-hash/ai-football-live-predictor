# telegram_bot

The active Telegram integration lives at [`../backend/app/telegram/`](../backend/app/telegram/) — this directory is reserved for a standalone bot worker (e.g. for interactive commands like `/signals_today`, `/settings`).
